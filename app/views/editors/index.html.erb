
<script type="text/javascript" src = "/assets/jquery.js"></script>

<%= javascript_tag do %>

 $('body').ready(function()
                       {
                            var editor = new Editor(null);
                            editor.init({
                                structure : "<%= @file_names.join(",") %>",
                                air : false,
                                toolbar : false,
                                textarea : false,
                                mode : 'visual',
                                content : "<%= @body.html_safe %>"
                            });
	});
<% end %>
