
<%= javascript_tag do %>
	$('body').ready(function()
	{
		var editor = new Editor(null);
                            editor.init({
                                air : false,
                                toolbar : false,
                                textarea : false,
                                mode : 'visual',
                                url : "<%= params[:file_link] %>"
                            });
	})
<% end %>
